name: Stage 2 Kinto GeckoTerminal Validation

on:
  push:
    paths:
      - 'scripts/windows/Verify-CfaStage2KintoGeckoTerminal.ps1'
      - 'candidate-analysis/CFA-Stage2-GeckoTerminal-Kinto-Seeds.csv'
      - '.github/workflows/stage2-kinto-geckoterminal-validation.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Parse Kinto verifier with PowerShell 7
        shell: pwsh
        run: |
          $tokens=$null; $errors=$null
          [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path '.\scripts\windows\Verify-CfaStage2KintoGeckoTerminal.ps1'),[ref]$tokens,[ref]$errors) | Out-Null
          if ($errors.Count -gt 0) { $errors | ForEach-Object { Write-Host $_.Message }; throw 'Kinto GeckoTerminal verifier failed PowerShell 7 parsing.' }

      - name: Parse Kinto verifier with Windows PowerShell 5.1
        shell: powershell
        run: |
          $tokens=$null; $errors=$null
          [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path '.\scripts\windows\Verify-CfaStage2KintoGeckoTerminal.ps1'),[ref]$tokens,[ref]$errors) | Out-Null
          if ($errors.Count -gt 0) { $errors | ForEach-Object { Write-Host $_.Message }; throw 'Kinto GeckoTerminal verifier failed Windows PowerShell 5.1 parsing.' }

      - name: Self-test Kinto verifier
        shell: powershell
        run: .\scripts\windows\Verify-CfaStage2KintoGeckoTerminal.ps1 -SelfTest

      - name: Acquire exact Kinto GeckoTerminal evidence
        shell: powershell
        run: .\scripts\windows\Verify-CfaStage2KintoGeckoTerminal.ps1

      - name: Upload Kinto GeckoTerminal evidence
        uses: actions/upload-artifact@v4
        with:
          name: cfa-stage2-kinto-geckoterminal-evidence
          path: |
            candidate-analysis/CFA-Stage2-GeckoTerminal-Kinto-Evidence.csv
            docs/evidence/stage2-kinto-geckoterminal-evidence.md
          if-no-files-found: error
          retention-days: 7
